apiVersion: v1
kind: PersistentVolume
metadata:
  name: openbao-data
  annotations:
    argocd.argoproj.io/sync-options: Prune=confirm
spec:
  capacity:
    storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: openbao-local
  local:
    path: /var/lib/rancher/rke2/storage/openbao
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - rke2-server
